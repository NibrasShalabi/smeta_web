import 'package:flutter/material.dart';

// TODO: replace with real widgets specific to the projects feature.
class ProjectsPlaceholderWidget extends StatelessWidget {
  const ProjectsPlaceholderWidget({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
