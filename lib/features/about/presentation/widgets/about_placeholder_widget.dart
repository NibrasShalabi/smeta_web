import 'package:flutter/material.dart';

// TODO: replace with real widgets specific to the about feature.
class AboutPlaceholderWidget extends StatelessWidget {
  const AboutPlaceholderWidget({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
