import 'package:flutter/material.dart';

// TODO: replace with real widgets specific to the contact feature.
class ContactPlaceholderWidget extends StatelessWidget {
  const ContactPlaceholderWidget({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
