import 'package:flutter/material.dart';

// TODO: replace with real widgets specific to the projects_management feature.
class ProjectsManagementPlaceholderWidget extends StatelessWidget {
  const ProjectsManagementPlaceholderWidget({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
