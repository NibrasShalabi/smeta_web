import 'package:flutter/material.dart';

// TODO: replace with real widgets specific to the estimator_pricing feature.
class EstimatorPricingPlaceholderWidget extends StatelessWidget {
  const EstimatorPricingPlaceholderWidget({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
