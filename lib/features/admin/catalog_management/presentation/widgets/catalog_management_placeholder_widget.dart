import 'package:flutter/material.dart';

// TODO: replace with real widgets specific to the catalog_management feature.
class CatalogManagementPlaceholderWidget extends StatelessWidget {
  const CatalogManagementPlaceholderWidget({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
