import 'package:flutter/material.dart';

// TODO: replace with real widgets specific to the estimator feature.
class EstimatorPlaceholderWidget extends StatelessWidget {
  const EstimatorPlaceholderWidget({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
