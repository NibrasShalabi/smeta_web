import 'package:flutter/material.dart';

// TODO: replace with real widgets specific to the catalog feature.
class CatalogPlaceholderWidget extends StatelessWidget {
  const CatalogPlaceholderWidget({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
